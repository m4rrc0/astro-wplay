---
import { Marked } from 'marked'
const marked = new Marked({
  async: true,
  pedantic: false,
  gfm: true,
})

const {
  props: { content, Element = 'div' },
} = Astro
---

<Element set:html={marked.parse(content)} />
